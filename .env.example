# Copy this file to ".env" in the PROJECT ROOT (not in notebooks/) and add your key.
#   cp .env.example .env
#
# Get a key at https://console.anthropic.com
# After creating or editing .env, RESTART the Deno kernel so the key is loaded.
ANTHROPIC_API_KEY=sk-ant-your-key-here
